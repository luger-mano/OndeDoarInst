import React from "react";

/*
  Deprecated legacy container removed (previously used jQuery + React.createClass).
  Keep a minimal placeholder here so importing this file won't break the build.
*/

export default function Container() {
  return null;
}